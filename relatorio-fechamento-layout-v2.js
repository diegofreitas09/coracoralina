(function(){
  const KEY='cora2027_fechamento_reajustes_v2';
  const money=v=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(Number(v)||0);
  const pct=v=>(Number(v)||0).toLocaleString('pt-BR',{minimumFractionDigits:2,maximumFractionDigits:2})+'%';
  const now=()=>new Date().toLocaleString('pt-BR',{timeZone:'America/Fortaleza'});
  const getDb=()=>{try{return JSON.parse(localStorage.getItem(KEY)||'{}')||{}}catch(e){return {}}};
  const bases=()=>{try{return (window.TUITION||TUITION||[]).map(r=>({segmento:r.segmento,due26:Number(r.due26)||0,late26:Number(r.late26)||0}))}catch(e){return []}};
  const mats=()=>{try{return window.MATERIALS||MATERIALS||[]}catch(e){return []}};
  const unis=()=>{try{return window.UNIFORMS||UNIFORMS||[]}catch(e){return []}};
  async function pdfLib(){if(window.jspdf?.jsPDF)return window.jspdf.jsPDF;await new Promise((res,rej)=>{const s=document.createElement('script');s.src='https://unpkg.com/jspdf@4.2.1/dist/jspdf.umd.min.js';s.onload=res;s.onerror=rej;document.head.appendChild(s)});return window.jspdf.jsPDF}
  async function img(src){try{const r=await fetch(src,{cache:'no-store'}),b=await r.blob();return await new Promise((res,rej)=>{const f=new FileReader();f.onload=()=>res(f.result);f.onerror=rej;f.readAsDataURL(b)})}catch(e){return null}}
  async function gerar(){
    const btn=document.getElementById('downloadFinalClosingPdf');if(!btn)return;const old=btn.innerHTML;
    try{btn.disabled=true;btn.textContent='Gerando relatório organizado...';const JsPDF=await pdfLib(),doc=new JsPDF({unit:'mm',format:'a4',compress:true});const W=210,M=14;const navy=[11,45,92],blue=[23,103,176],green=[22,128,71],gray=[88,112,139],light=[242,247,252],amber=[166,107,0];let y=16;
      const logo=await img('logo-escola-web.png'),consult=await img('logo-pdf-web.png');
      function footer(){const p=doc.getNumberOfPages();doc.setPage(p);doc.setFontSize(7);doc.setTextColor(130,145,160);doc.text('Colégio Cora Coralina • Fechamento 2027',M,291);doc.text('Página '+p,W-M,291,{align:'right'})}
      function newPage(title,sub){footer();doc.addPage();y=18;doc.setFillColor(...navy);doc.roundedRect(M,y,W-2*M,18,3,3,'F');doc.setTextColor(255);doc.setFont('helvetica','bold');doc.setFontSize(13);doc.text(title,M+6,y+7);doc.setFont('helvetica','normal');doc.setFontSize(8);if(sub)doc.text(sub,M+6,y+13);y+=25}
      function sectionTitle(t){doc.setTextColor(...navy);doc.setFont('helvetica','bold');doc.setFontSize(11);doc.text(t,M,y);y+=5;doc.setDrawColor(200,216,232);doc.line(M,y,W-M,y);y+=5}
      function row(cols,widths,head=false){const h=8;if(y+h>280)newPage('CONTINUAÇÃO DO RELATÓRIO','Tabela continuada');let x=M;if(head){doc.setFillColor(...blue);doc.rect(M,y,W-2*M,h,'F');doc.setTextColor(255);doc.setFont('helvetica','bold')}else{doc.setTextColor(42,58,74);doc.setFont('helvetica','normal');if(Math.round(y/8)%2===0){doc.setFillColor(249,251,254);doc.rect(M,y,W-2*M,h,'F')}}doc.setFontSize(7.1);cols.forEach((t,i)=>{doc.text(String(t),x+2,y+5.2,{maxWidth:widths[i]-4});x+=widths[i]});y+=h}
      if(logo)doc.addImage(logo,'PNG',M,y,23,23,undefined,'FAST');if(consult)doc.addImage(consult,'PNG',170,y,23,23,undefined,'FAST');doc.setTextColor(...navy);doc.setFont('helvetica','bold');doc.setFontSize(15);doc.text('COLÉGIO CORA CORALINA',42,y+7);doc.setFontSize(21);doc.text('Fechamento Oficial 2027',42,y+17);doc.setFont('helvetica','normal');doc.setFontSize(8.5);doc.setTextColor(...gray);doc.text('Gerenciador de produtos, valores, mensalidades e condições comerciais',42,y+23);y=48;doc.setFillColor(...navy);doc.roundedRect(M,y,W-2*M,24,4,4,'F');doc.setTextColor(255);doc.setFont('helvetica','bold');doc.setFontSize(14);doc.text('RELATÓRIO FINAL DE FECHAMENTO',M+7,y+9);doc.setFont('helvetica','normal');doc.setFontSize(9);doc.text('Matrículas 2027 • Consolidado da gestão',M+7,y+16);doc.setFontSize(7.5);doc.text('Gerado em '+now(),M+7,y+21);
      const D=getDb();D.tuition=D.tuition||{};D.records=D.records||{};const B=bases(),all=[...mats().map(r=>'L|'+r.segment+'|'+r.name),...unis().map((r,i)=>'F|'+i+'|'+r.segment+'|'+r.name)];const savedT=B.filter(x=>D.tuition[x.segmento]?.saved).length,savedI=all.filter(id=>D.records[id]?.saved).length,total=B.length+all.length,saved=savedT+savedI;y=82;sectionTitle('VISÃO GERAL DO FECHAMENTO');[['Segmentos',B.length],['Registros',total],['Salvos',saved],['Pendentes',total-saved]].forEach((c,i)=>{const x=M+i*45;doc.setFillColor(...light);doc.roundedRect(x,y,41,20,2,2,'F');doc.setTextColor(...gray);doc.setFontSize(7);doc.text(c[0],x+4,y+6);doc.setTextColor(...navy);doc.setFont('helvetica','bold');doc.setFontSize(14);doc.text(String(c[1]),x+4,y+15)});y+=30;doc.setFillColor(saved===total?236:255,saved===total?247:247,saved===total?241:226);doc.roundedRect(M,y,W-2*M,23,3,3,'F');doc.setTextColor(...navy);doc.setFontSize(10);doc.text(saved===total?'FECHAMENTO COMPLETO':'FECHAMENTO PARCIAL',M+6,y+8);doc.setFont('helvetica','normal');doc.setTextColor(...gray);doc.setFontSize(8);doc.text(saved+' de '+total+' registros salvos; '+(total-saved)+' pendentes.',M+6,y+15);

      newPage('1. MENSALIDADES E PLANOS 2027','Anuidade, reajuste, 1ª parcela e condições Plano A / Plano B');row(['Segmento','Anuidade 2026','Reajuste','Anuidade 2027','1ª parcela','Plano A 12x','Plano B 11x','Status'],[34,25,17,27,23,24,24,18],true);B.forEach(base=>{const r=D.tuition[base.segmento]||{},rate=Number(r.rate)||0,ann=Number(r.annuity)||base.due26*(1+rate/100),first=Number(r.first)||0,a=Number(r.planA)||Math.max(0,ann-first)/12,b=Number(r.planB)||Math.max(0,ann-first)/11;row([base.segmento,money(base.due26),pct(rate),money(ann),money(first),money(a),money(b),r.saved?'SALVO':'PENDENTE'],[34,25,17,27,23,24,24,18])});y+=8;sectionTitle('ANUIDADE APÓS O VENCIMENTO');row(['Segmento','2026 após venc.','Reajuste','2027 após venc.'],[48,47,28,59],true);B.forEach(base=>{const r=D.tuition[base.segmento]||{},rate=Number(r.rate)||0;row([base.segmento,money(base.late26),pct(rate),money(base.late26*(1+rate/100))],[48,47,28,59])});

      newPage('2. MATERIAIS E LIVROS','Valores-base, reajustes e valores fechados para 2027');row(['Segmento','Produto','2026','Reajuste','2027','Status'],[38,62,25,20,25,22],true);mats().forEach(r=>{const id='L|'+r.segment+'|'+r.name,s=D.records[id]||{},rate=Number(s.rate??0),v=Number(s.v27)||Number(r.v26)*(1+rate/100);row([r.segment,r.name,money(r.v26),pct(rate),money(v),s.saved?'SALVO':'PENDENTE'],[38,62,25,20,25,22])});

      newPage('3. FARDAMENTO','Valores oficiais por segmento e produto');row(['Segmento','Produto','2026','Reajuste','2027','Status'],[38,62,25,20,25,22],true);unis().forEach((r,i)=>{const id='F|'+i+'|'+r.segment+'|'+r.name,s=D.records[id]||{},rate=Number(s.rate??0),v=Number(s.v27)||Number(r.v26)*(1+rate/100);row([r.segment,r.name,money(r.v26),pct(rate),money(v),s.saved?'SALVO':'PENDENTE'],[38,62,25,20,25,22])});

      newPage('4. SITUAÇÃO FINAL E APROVAÇÃO','Conferência administrativa antes da publicação para as famílias');doc.setFillColor(saved===total?236:255,saved===total?247:247,saved===total?241:226);doc.roundedRect(M,y,W-2*M,35,4,4,'F');doc.setTextColor(saved===total?green:amber);doc.setFont('helvetica','bold');doc.setFontSize(15);doc.text(saved===total?'FECHAMENTO COMPLETO':'FECHAMENTO PARCIAL',M+7,y+12);doc.setTextColor(...navy);doc.setFontSize(10);doc.text(saved+' registros aprovados de '+total,M+7,y+21);doc.setFont('helvetica','normal');doc.setTextColor(...gray);doc.setFontSize(8.5);doc.text((total-saved)+' registro(s) ainda pendente(s) de fechamento.',M+7,y+28);y+=55;doc.setTextColor(...navy);doc.setFont('helvetica','bold');doc.setFontSize(10);doc.text('APROVAÇÕES',M,y);y+=20;doc.setDrawColor(145,165,185);doc.line(M,y,M+78,y);doc.line(W-M-78,y,W-M,y);doc.setFont('helvetica','normal');doc.setFontSize(8);doc.setTextColor(...gray);doc.text('Responsável pela elaboração',M,y+6);doc.text('Direção / Financeiro',W-M-78,y+6);y+=22;doc.setFillColor(...light);doc.roundedRect(M,y,W-2*M,26,3,3,'F');doc.setTextColor(...navy);doc.setFont('helvetica','bold');doc.setFontSize(9);doc.text('Próxima etapa operacional',M+6,y+8);doc.setFont('helvetica','normal');doc.setTextColor(...gray);doc.setFontSize(8);doc.text('Após a aprovação, os valores e produtos podem ser publicados no Cora Família e usados nos panfletos por turma.',M+6,y+16,{maxWidth:W-2*M-12});
      for(let p=1;p<=doc.getNumberOfPages();p++){doc.setPage(p);doc.setFontSize(7);doc.setTextColor(130,145,160);doc.text('Colégio Cora Coralina • Fechamento 2027',M,291);doc.text('Página '+p+' de '+doc.getNumberOfPages(),W-M,291,{align:'right'})}doc.save('Relatorio-Final-Fechamento-2027-Organizado.pdf')
    }catch(e){console.error(e);alert('Não foi possível gerar o relatório final agora.')}finally{btn.disabled=false;btn.innerHTML=old}
  }
  function bind(){const b=document.getElementById('downloadFinalClosingPdf');if(b&&!b.dataset.layoutV2){b.dataset.layoutV2='1';b.onclick=gerar}}
  function init(){bind();new MutationObserver(bind).observe(document.body,{childList:true,subtree:true})}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();